apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "neon-postgres-chart.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "neon-postgres-chart.serviceAccountName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "neon-postgres-chart.fullname" . }}
